<view class="tab">
    <block  wx:for="{{titles}}" wx:key="index">
        <view class="tab-item {{isActive==index?'active':''}}"
              bindtap="ontap"
              data-index="{{index}}">
            <text>{{item}}</text>
        </view>
    </block>
</view>